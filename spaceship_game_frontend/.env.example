# PUBLIC_INTERFACE
# REACT_APP_LEADERBOARD_API should point to the backend base URL exposing:
#  - GET  /api/leaderboard/top
#  - POST /api/leaderboard/submit
# If omitted, a localStorage-backed mock leaderboard is used.
REACT_APP_LEADERBOARD_API=
